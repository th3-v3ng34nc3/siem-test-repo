apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-sqlite-pvc
  namespace: dashboard-go-yubi
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi # SQLite is very lightweight; 2GB is plenty for most