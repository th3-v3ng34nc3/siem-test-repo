apiVersion: v1
kind: Service
metadata:
  name: aws-cloudtrail
  namespace: tenant-yoma-fleet
  annotations:
    tenant: yoma-fleet
    collector: cloudtrail
    type: aws
spec:
  selector:
    app: aws-cloudtrail
  ports:
  - port: 9090
    targetPort: 9090
    name: tcp
