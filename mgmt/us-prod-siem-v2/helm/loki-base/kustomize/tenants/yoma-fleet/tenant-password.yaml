apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tenant-password
  namespace: tenant-yoma-fleet
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend # AWS, Vault, etc.
    kind: ClusterSecretStore
  target:
    template:
      engineVersion: v2
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: tenants/yoma-fleet/loki_tenant
        property: username
    - secretKey: password
      remoteRef:
        key: tenants/yoma-fleet/loki_tenant
        property: password