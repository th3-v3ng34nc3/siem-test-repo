apiVersion: v1
kind: Service
metadata:
  name: aws-aws
  namespace: tenant-seran
  annotations:
    tenant: seran
    collector: aws
    type: aws
spec:
  selector:
    app: aws-aws
  ports:
  - port: 9090
    targetPort: 9090
    name: tcp
