apiVersion: apps/v1
kind: Deployment
metadata:
  name: azure-<intergation_id>
  namespace: tenant-<tenant_id>
  labels:
    tenant: <tenant_id>
    collector: <intergation_id>
    type: azure
spec:
  selector:
    matchLabels:
      app: azure-<intergation_id>
  template:
    metadata:
      labels:
        app: azure-<intergation_id>
    spec:
      volumes:
        - name:  azure-alloy-<intergation_id>
          secret:
            secretName:  azure-alloy-<intergation_id>
        - name: azure-logstash-<intergation_id>
          secret:
            secretName: azure-logstash-<intergation_id>
      containers:
      - name: alloy
        image: grafana/alloy:v1.12.2
        imagePullPolicy: IfNotPresent
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        args:
        - run
        - /etc/alloy/config.alloy
        - --storage.path=/var/lib/alloy/data
        - --disable-reporting
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        volumeMounts:
          - name: azure-alloy-<intergation_id>
            readOnly: true
            mountPath: /etc/alloy/
      - name: logstash
        image: public.ecr.aws/k9v9d5v2/admin-tools/crd-collectors/logstash:v1.0.1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9091
        readinessProbe:
          httpGet:
            path: /
            port: 9091
          initialDelaySeconds: 20
          periodSeconds: 10
        args:
          - "-f"
          - "/configs/pipeline.conf"
        volumeMounts:
          - name: azure-logstash-<intergation_id>
            readOnly: true
            mountPath: /configs/
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m" 
